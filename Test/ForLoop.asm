; ----------------------------------------------------------------------------
; i64 i = 0
; while (i < 10)
;    i++
;    print i
; ----------------------------------------------------------------------------
.text

main:
    mov x0, 0
    mov x1, 0xFF
top:
    cmp x0, x1
    jge done
    inc x0
    jmp top
done:
    prg x0
    mov x0, 0
    ret
